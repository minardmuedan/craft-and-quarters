export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <section className="min-h-dvhMinusNav flex flex-col items-center justify-center py-5">{children}</section>
}
